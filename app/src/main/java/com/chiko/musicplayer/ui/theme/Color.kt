package com.chiko.musicplayer.ui.theme

import androidx.compose.ui.graphics.Color

// Monochrome palette — keeping the old names so call-sites don't need to change.
val NeonViolet = Color(0xFFEDEDED)       // accent: near-white
val NeonCyan = Color(0xFFBDBDBD)         // secondary accent: light grey
val DeepPurple = Color(0xFF0F0F0F)
val MidnightBlue = Color(0xFF050505)
val Charcoal = Color(0xFF141414)
val SoftWhite = Color(0xFFF1F1F1)
val MutedLavender = Color(0xFFA8A8A8)    // onSurfaceVariant: muted grey
val SurfaceCard = Color(0xFF1A1A1A)
val SurfaceCardElevated = Color(0xFF262626)
